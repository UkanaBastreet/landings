import { Metadata } from "next";
import { FC } from "react";

export const meta: Metadata = {
  title: "Agency",
};

const page: FC = () => {
  return (
    <main className="agency">
        <header></header>
    </main>
  )
};

export default page;
